<h1> All Users </h1>

<div>
  <ul>
    <% @users.each do |user| %>
      <li> <%= link_to user.name.titleize, user_path(user) %> </li>
    <% end %>
  </ul>
</div>

<%= link_to "Create New User", new_user_path %>
